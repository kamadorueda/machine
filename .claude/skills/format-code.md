# Format Code

Formats Nix and shell files in the repository.

```bash
scripts/format
```

**Run this after modifying any `.nix` or `.sh` files** to keep code consistently formatted before building or committing.
